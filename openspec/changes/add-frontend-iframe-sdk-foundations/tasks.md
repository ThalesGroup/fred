## 1. Tracking and compatibility baseline

- [x] 1.1 Create or confirm the dedicated GitHub tracking issue, link it from `proposal.md`, and verify neither closed package-foundation issue was repurposed. (`#2614`; duplicate search returned only closed `#2583` and `#2590`.)
- [x] 1.2 Inventory every import and caller of `applicationHost.ts`, `applicationPath.ts`, and the protected-header/request helpers, including raw or dynamically constructed protocol messages; record the intended compatibility re-exports and verify the inventory with repository-wide searches.
- [x] 1.3 Add golden protocol-`"1"` tests for all eight current serialized message shapes, optional-field normalizations, six methods, ID/header limits, and malformed inputs; verify the tests pass against the pre-extraction behavior before moving definitions.
- [x] 1.4 Record the audited RFC discrepancies in the implementation documentation plan—illustrative API, deferred theme/live locale, buffered text transport, ordinary HTTP(S) origins, and no opaque/`null` origin—and verify no task relies on a stale RFC statement.

## 2. Canonical protocol extraction

- [x] 2.1 Create the transport-neutral canonical `applicationProtocol.ts` with protocol constants, cloneable context/message types, request methods and limits, and pure frame/host parsers; verify it imports no React, router, authentication, backend, catalog, or host-state module.
- [x] 2.2 Move the existing relative-path validation and protected-header identification into the canonical protocol boundary while preserving compatibility re-exports from the current modules; verify all current callers compile without application behavior loss.
- [x] 2.3 Update `applicationHost.ts`, `applicationPath.ts`, and `applicationRequest.ts` to consume the canonical definitions, and verify golden host/path/request tests preserve exact normalization, rejection, and header behavior.
- [x] 2.4 Extend parser and path tests for host-to-frame messages, complete context validation, unsupported versions, malformed nested values, encoded traversal, and protected-header casing; verify pure validation rejects each mutation without browser or host state.

## 3. Framework-independent child client

- [x] 3.1 Add the `@fred/iframe-sdk` workspace member, public root/protocol source boundaries, development manifest, FRED license, and README skeleton; verify the manifest exposes only `.` and `./protocol` and declares no dependencies or peer dependencies.
- [x] 3.2 Implement origin/application configuration and the shared `connect()` lifecycle with listener-before-ready ordering, immediate plus 500 ms ready retries, a default 10-second deadline, exact parent/source/origin admission, and stable typed failures; verify focused unit tests cover valid, raced, concurrent, malformed, mismatched, timed-out, missing-parent, and disposed connection cases.
- [x] 3.3 Implement immutable context access and route subscriptions with idempotent unsubscribe; verify every accepted route event is delivered once to each current subscriber, identical `subPath` values are not deduplicated, and wrong-origin/window, pre-context, malformed, and post-disposal messages cannot notify consumers.
- [x] 3.4 Implement validated `navigate()` and `openChat()` intents with normalized replace and nullable session fields; verify valid wire shapes and local rejection of every unsafe path class before any postMessage call.
- [x] 3.5 Implement request serialization with collision-checked IDs, allowed methods, string-or-null bodies, ordinary string headers, case-insensitive protected-header rejection, and the 16-request client bound; verify invalid requests and the seventeenth pending request fail locally without posting.
- [x] 3.6 Implement out-of-order response correlation, default/per-request 30-second deadlines, `AbortSignal`, generic transport errors, and duplicate/unknown/late-response suppression; verify cancellation releases only local state and no mutation is retried.
- [x] 3.7 Reconstruct browser `Response` objects for success and non-OK HTTP statuses, using null bodies for HEAD and 204/205/304; verify JSON/text, 401/403/5xx, headers, transport-error, and every bodyless case.
- [x] 3.8 Implement idempotent disposal that rejects connection and pending work and removes listeners, timers, abort hooks, and subscribers; verify subsequent calls and messages from the disposed lifecycle have no effect.

## 4. FRED host and legacy-client compatibility

- [x] 4.1 Update canonical host-page tests to exercise legacy raw clients after extraction and the new canonical protocol surface; verify all eight existing shapes retain their serialized fields, defaults, and effects without a version change.
- [x] 4.2 Add direct SDK/current-host compatibility coverage that loads the actual generated tarball and mounts `TeamApplicationHostPage` or exercises its production message handler for ready/context, route, navigation, open-chat, requests, HTTP and transport errors, pending limits, and stale frame/team teardown. Include host route A, child navigation to B, then host route A; verify both accepted A events reach current subscribers, request responses remain deduplicated by request ID, the host needs no protocol adapter, and the SDK sends no operation before context. Retain the raw legacy-client and simulated cross-origin suites as separate gates. Evidence: `make host-integration` validates and extracts the actual tarball, mounts the production host page, and passes four cases covering the full matrix plus frame-target and team replacement; the existing raw and browser suites remain separate.
- [x] 4.3 Extend frame/team lifecycle tests for application, target, team, and unmount replacement; verify old listeners are removed, fetches are aborted, late replies are suppressed, and stale frames cannot affect the new lifecycle.
- [x] 4.4 Run focused application host, request, path, TeamApplicationHostPage, authorization/token-refresh, and proxy suites; verify the 15-second host deadline, 16-request host bound, source/origin checks, protected-header defense, bearer/refresh policy, route ownership, and proxy behavior remain green.

## 5. SDK generation and archive validation

- [x] 5.1 Extend the producer's exact input inventory and generator to copy the canonical protocol and allowlisted client sources into disposable generated input; verify recorded paths/hashes and the reviewed module graph fail on missing, extra, or changed inputs.
- [x] 5.2 Build deterministic ESM JavaScript and closed declarations for both public entry points; verify positive build fixtures contain runtime imports/exports that close over executable packed modules and declaration references/`types` exports that close over packed `.d.ts` files, with no bundled Node code, framework/FRED dependency, source alias, absolute path, or undeclared module.
- [x] 5.3 Add an SDK archive validator using the shared tar safety checks and distinct runtime and declaration resolvers: runtime imports/exports must reach executable packed modules, while declaration references and `types` exports may reach valid packed `.d.ts` files; verify positive tests for both resolution paths and the real `npm pack` archive pass.
- [x] 5.4 Add disposable negative archive mutations for missing/extra files, unsafe paths or links, wrong exports/metadata/license/evidence, runtime imports or exports with only declaration targets, declaration references or `types` exports without valid `.d.ts` targets, checkout/workspace references, local protocols, and undeclared or framework dependencies; verify each mutation fails for its intended resolver and reason.
- [x] 5.5 Run the combined producer generation, quality, unit, pack, and archive suites; verify all existing token/UI positive and negative assertions pass unchanged alongside the SDK.

## 6. Isolated neutral consumer

- [x] 6.1 Add a framework-neutral TypeScript/browser fixture and lockfile that imports the client root and protocol entry point without React or another FRED package; verify its dependency graph contains only the declared pinned tooling and the staged SDK archive.
- [x] 6.2 Extend provisioning to populate a dedicated cache from the fixture lockfile and report the pinned browser-install command separately; verify provisioning is the only consumer step permitted to access the network.
- [x] 6.3 Install the actual SDK tarball and cached tooling into a fresh OS temporary directory outside FRED using npm offline mode, then type-check and build; verify there are no workspace/local links, FRED checkout paths, FRED `node_modules` resolution, React, or network fetches.
- [x] 6.4 Add missing-cache and missing-browser prerequisite checks; verify offline validation and browser smoke fail actionably instead of downloading, using FRED dependencies, or bootstrapping Chromium.

## 7. Cross-origin browser validation

- [x] 7.1 Extend the existing harness with separate loopback host and child servers, stage only the isolated build and packed protocol surface, and load the child through a real iframe; verify browser origin and `WindowProxy` identities are distinct and all resource responses are successful and local.
- [x] 7.2 Add browser cases for ready retry, matching context, route subscription, host route A followed by child navigation to B and host route A again, navigation, open-chat, and two out-of-order requests; verify each accepted route message is delivered once without sub-path deduplication while response settlement remains deduplicated by request ID in the installed SDK.
- [x] 7.3 Add browser cases for buffered JSON/text success, 401/403/5xx Responses, generic transport failure, and HEAD/204/205/304 bodyless Responses; verify Fetch-like observable results without streaming or binary claims.
- [x] 7.4 Add browser cases for malformed messages, unsupported context, wrong application ID, wrong origin/window, sibling/popup/stale frame impersonation, and unsafe paths/headers; verify no unadmitted event changes client or host state.
- [x] 7.5 Add browser cases for pending capacity, connection/request deadlines, local abort, duplicate/unknown/late replies, disposal, and frame/team replacement; verify local resources are released and old lifecycles cannot affect replacements.
- [x] 7.6 Make browser request monitoring reject non-loopback, `file:`, external-service, checkout-path, failed, and non-successful resource requests, and verify smoke performs no installation or browser provisioning.

## 8. Commands, CI selection, and documentation

- [x] 8.1 Add producer/root commands for SDK generation, unit tests, packing, archive checks, isolated consumption, and browser smoke while preserving existing aggregate commands; verify each focused command and the aggregate frontend-package gate execute the intended stages exactly once.
- [x] 8.2 Extend CI input classification for every SDK source, canonical protocol/path input, producer manifest/lockfile, build/validator/shared archive script, fixture/lockfile, browser harness, license, README, Makefile, and workflow input; verify table-driven positive selection tests cover each class.
- [x] 8.3 Preserve negative CI-selection behavior for unrelated application files and require both package and application gates for canonical protocol/path or declared host-compatibility inputs; verify selector tests distinguish these cases exactly.
- [x] 8.4 Complete the SDK consumer README and producer README with public API, explicit origin/application configuration, buffered text/JSON semantics, limits, local-only timeout/cancel behavior, host authority, offline/provision commands, and unsupported features; verify every documented import resolves from the packed archive.
- [x] 8.5 Update the durable control-plane application-hosting contract and affected frontend guidance to identify the canonical protocol source and SDK boundary; verify authentication, authorization, routing, refresh, and proxy ownership remain assigned to FRED.
- [x] 8.6 Correct only the existing frontend-packaging RFC's newly stale implementation-status and suggested-location statements while leaving theme/live locale, publication, registry adoption, and external adopter work open; verify links still target the single existing RFC.

## 9. Completion evidence

- [x] 9.1 Run the repository-pinned producer install/provisioning, quality, unit, deterministic generation, pack, token/UI/SDK archive mutation, isolated offline-consumer, cross-origin browser, and direct packed-SDK/production-host commands; record exact commands and successful results in this checklist without conflating provisioning with offline execution. Evidence: the separate `make consumer-provision` and `make browser-install` phase populated both pinned caches and confirmed Chromium; `make code-quality` passed; the final `make test` passed all 174 producer/build/archive mutation tests; `make pack-check` validated all three real archives; `make isolated-consumer` installed, type-checked, and built all consumers in npm offline mode; `make host-integration` passed four actual-tarball/production-host cases; and `make browser-smoke` passed with zero installations, zero browser provisioning, and zero external requests. The first sandboxed provisioning and browser attempts failed respectively on blocked DNS and loopback binding, then passed with the required permissions without changing validation behavior.
- [x] 9.2 Run the repository-pinned frontend quality, production build, complete tests, and focused host/request/path/page/proxy/authentication regressions; record exact commands and results, or an actionable environmental blocker without weakening acceptance. Evidence: from `apps/frontend`, `make code-quality` passed; `make build` passed with 5,547 modules; `make test` passed 206 files and 2,114 tests with 2 files/6 tests intentionally skipped, including the archive-backed integration that passed separately; the focused five-file protocol/host/path/request/page command passed 142 tests; and `npm run applications:test` passed all 9 proxy tests plus proxy smoke. The repository-pinned Node 22.13.0 emitted existing engine-range warnings for newer jsdom/undici packages but every gate completed successfully.
- [x] 9.3 Run strict OpenSpec validation for `add-frontend-iframe-sdk-foundations` and `git diff --check`; verify both pass and all implementation evidence is linked before checking the final task. Evidence: both commands passed after the implementation, hardening evidence, and review resolution were recorded.
- [x] 9.4 Obtain the repository-required independent implementation review, resolve every in-scope finding, and record findings/resolutions; verify no package was published, no registry/RAGS/SDK adoption occurred, and the change and tracking issue remain open for review. Evidence: the first `codex review --uncommitted` found that TypeScript parse diagnostics alone admitted grammar-invalid runtime JavaScript such as top-level `return`; disposable mutations reproduced that gap, and every executable now passes Node's parse-only native ESM grammar check before reference resolution. The final independent re-review found no actionable correctness issues and independently passed package tests, host integration, TypeScript checks, formatting, and strict OpenSpec validation. Issue `#2614` and this change remain open; no publication, registry/FRED/RAGS adoption, merge, or archive occurred.

## 10. Review hardening

- [x] 10.1 Add disposable archive regressions that first reproduce commented static runtime and declaration imports, literal-template dynamic imports, computed dynamic imports, malformed syntax, extensionless runtime imports, and directory-index fallback; verify the pre-fix validator accepts or misses each applicable bad reference before changing validation code. Evidence: the focused pre-fix run failed seven new rejection tests with `Missing expected rejection`; its native Node ESM control independently rejected the extensionless import.
- [x] 10.2 Replace regex reference discovery with a shared TypeScript-compiler-API parser covering static imports, re-exports, dynamic imports, declaration import types, and reference directives; reject computed runtime imports and parse diagnostics, and use the same scanner for build evidence. Evidence: `module-references.test.mjs` covers every syntax class and both invalid forms; validator and generator import the same scanner.
- [x] 10.3 Enforce exact native ESM runtime targets without extension inference or directory-index fallback while retaining declaration-aware `.d.ts` resolution; add positive and negative tests for both paths plus a native ESM loading regression. Evidence: focused archive tests accept explicit `./protocol.js` and packed declarations, reject extensionless/directory/declaration-only runtime targets, and load the actual root entry with Node native ESM.
- [x] 10.4 Complete task 4.2's direct actual-tarball/production-host integration matrix and extend CI-selection tests for its production source, fixture, orchestration, and packed-SDK inputs. Evidence: four integration cases pass; the declared input inventory and workflow include the new frontend test and gate, and the table-driven CI test verifies the frontend dependency install and `make host-integration` step.
- [x] 10.5 Update producer and SDK documentation to describe syntax-aware archive inspection, exact runtime specifiers, the direct host integration gate, and the distinct purposes of raw-host, simulated cross-origin, and direct compatibility suites. Evidence: the producer and package READMEs document the parser, exact native ESM rule, provisioning boundary, and three complementary compatibility suites.
- [x] 10.6 Re-run all producer, archive, isolated-consumer, browser, frontend, OpenSpec, and diff gates after hardening, then obtain independent review and resolve every in-scope finding before restoring completion evidence. Evidence: producer quality, 174 tests, all three archive checks, all three isolated consumers, the four-case production-host integration, cross-origin browser smoke, frontend quality/build/full and focused regressions, strict OpenSpec validation, and `git diff --check` passed. The independent grammar-validation finding was fixed, its focused regressions passed, and the final re-review was clean.

## 11. Fixture review corrections

- [x] 11.1 Reproduce the host fixture accepting unsafe query-configured iframe destinations, then add positive and negative regressions for dynamic loopback ports, executable and unsupported schemes, external and misleading hosts, credentials, unexpected URL components, invalid ports, malformed/colliding origins, and rejection before either iframe navigation. Evidence: the pre-fix browser regression observed both iframe `src` attributes populated from a `javascript:` application origin instead of the expected `{ application: null, attacker: null }`; the focused post-fix suite passes 19 table-driven origin/configuration cases and the browser regression confirms both unsafe application and attacker configuration are rejected before either navigation.
- [x] 11.2 Implement one fixture-only origin parser and fixed URL-construction path for initial application, attacker, and replacement frames; verify all three origins remain distinct and no arbitrary configured string becomes a navigation target without narrowing production SDK origin support. Evidence: `fixture-origin.ts` accepts only canonical `http://127.0.0.1:<dynamic-port>` fixture origins, validates application and attacker configuration before constructing either destination, and constructs fixed `/child.html` and `/attacker.html` URLs through one helper; browser evidence records three distinct origins and checks the initial and replacement child origin/path.
- [x] 11.3 Move both readonly context/route assignments into a dedicated typecheck-only source included by `tsconfig.json` but excluded from every browser entry/import graph; verify the negative assertions are consumed and neither marker appears in built JavaScript. Evidence: offline consumer TypeScript validation consumes both `@ts-expect-error` assertions; its source audit requires the dedicated fixture and both markers, while its staged-output audit rejects either marker in built JavaScript. The actual archive consumer type-checks and builds successfully.
- [x] 11.4 Remove the unused attacker reassignment while retaining attacker admission tests, route A → child B → host A, replacement, capacity, disposal, and local-successful request assertions in browser smoke. Evidence: the replacement destructuring now updates only `child`; `make browser-smoke` passes the attacker admission, `["A", "A"]` route, replacement, capacity, disposal, response, and local-successful request assertions with zero external requests.
- [x] 11.5 Run producer quality, unit, archive, offline isolated-consumer, production-host integration, and browser gates; run strict OpenSpec validation and `git diff --check`; obtain independent review, resolve in-scope findings, and distinguish local evidence from CodeQL results that require a pushed revision. Evidence: `make code-quality` passed; the final `make test` passed 193 tests; `make pack-check` validated the design-token, UI, and iframe-SDK archives; `make isolated-consumer` installed and built all three consumers offline; `make host-integration` passed four actual-tarball/production-host cases; and `make browser-smoke` passed with three distinct origins, both unsafe-origin cases rejected before navigation, successful admission, attacker rejection, route A → child B → host A, replacement/disposal, zero installs or provisioning, and zero external requests. Strict OpenSpec validation and `git diff --check` passed. Two independent `codex review --uncommitted` passes found no actionable issue and independently passed focused tests, lint, formatting, and diff/OpenSpec checks; their sandbox could not bind loopback ports, while the local browser gate passed. GitHub CodeQL status remains unverified until a later pushed revision runs remote analysis.
