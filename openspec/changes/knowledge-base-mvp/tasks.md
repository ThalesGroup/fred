Groups 1-6 are already implemented and merged on this feature's development branch, recorded here as done with their verification, not proposed. Groups 7-9 (2026-09-07 addition, design.md D8-D10) come next, before Group 10 — the developer requires the ingestion hand-off to be decoupled from `knowledge-flow-backend` internals, and both MVP knowledge base types provably sharing it, before any further pull-specific work continues. Group 11 depends on Group 10 landing, and (2026-09-07 review pass) is additionally blocked on design.md's open D9/D13 scheduler-backend decision — see Group 11's own note. Do not batch remaining groups into one uninterrupted pass — each stays its own scoped commit, per this project's convention.

## 1. Core contracts (`fred-sdk`)

- [x] 1.1 Add `KnowledgeBaseType`, `KnowledgeBase`, `KnowledgeBaseScope`, `KnowledgeBaseMode`, `KnowledgeBaseKind` to `libs/fred-sdk/fred_sdk/contracts/knowledge_base.py`, with the mode/connector_kind exclusivity invariant on `KnowledgeBaseType`. Verify: `libs/fred-sdk/tests/test_knowledge_base_contracts.py` (13 tests) passes; `make code-quality` clean.
- [x] 1.2 Add `SourceItem`, `SourceChange`, `ChangeKind`, `SourceConnector` to `libs/fred-sdk/fred_sdk/contracts/connector.py`. Verify: same test file, including the in-memory fake proving the Protocol is implementable.

## 2. First `SourceConnector` implementation

- [x] 2.1 Implement `LocalFilesystemConnector` (`apps/knowledge-flow-backend/knowledge_flow_backend/core/connectors/local_filesystem_connector.py`) — content-hash revision, not mtime. Verify: `tests/core/connectors/test_local_filesystem_connector.py` (6 tests) passes against a real temporary directory, including the case where content changes but mtime does not.

## 3. Sync wiring into the existing push pipeline

- [x] 3.1 Implement `sync_pull_knowledge_base` (`apps/knowledge-flow-backend/knowledge_flow_backend/features/scheduler/pull_knowledge_base_sync.py`), reusing `extract_metadata`/`save_input`, `push_input_process`, `output_process`, `delete_document_and_artifacts`. Verify: `tests/features/scheduler/test_pull_knowledge_base_sync.py` (6 tests, mocked ingestion primitives) covers upsert, delete, changed-content (delete-then-recreate), and no-op.

## 4. Prior cleanup (predates this design, listed for the record)

- [x] 4.1 Removed untested/dead pull-mode scaffolding that predated this design (old content loaders, dead catalog store, unwired Sphere/GitHub/Gitlab config, unreachable branches this left behind). Verify: full offline test suite green at the time, no behavior loss (nothing removed had test coverage).

## 5. `knowledge_base_type` usage-enablement gate (`fred-core`)

- [x] 5.1 Add `type knowledge_base_type` to `libs/fred-core/fred_core/security/rebac/schema.fga` (organization/default_on/enabled/disabled/can_manage/can_use; no personal-space overlay), regenerate `schema.fga.json` via `make transform-openfga-schema`. Verify: JSON regeneration diff is scoped to the addition only.
- [x] 5.2 Add `Resource.KNOWLEDGE_BASE_TYPE`, `KnowledgeBaseTypePermission`, and `can_team_use_knowledge_base_type` (`fred_core/security/rebac/knowledge_base_type_authz.py`). Verify: `fred_core/tests/security/test_knowledge_base_type_authz.py` (3 tests) passes.

## 6. `KnowledgeBaseType` config and `KnowledgeBase` persistence (`control-plane-backend`)

- [x] 6.1 Add `KnowledgeBaseTypeConfig` as deployment configuration (`control_plane_backend/knowledge_base_types/catalog.py`), subclassing `fred_sdk.contracts.knowledge_base.KnowledgeBaseType` rather than redeclaring its fields, mirroring `ApplicationSourceConfig`. Verify: `tests/test_knowledge_base_types.py` (3 tests).
- [x] 6.2 Add the `knowledge_base` table (`control_plane_backend/models/knowledge_base_models.py`) and `KnowledgeBaseStore` (`control_plane_backend/knowledge_base/store.py`), reading/writing `fred_sdk.contracts.knowledge_base.KnowledgeBase` directly. Verify: `tests/test_knowledge_base_store.py` (4 tests, real SQLite engine); migration `alembic/versions/57f8e7e05006_add_knowledge_base.py` passes `make db-check-heads` and `make db-check-sqlite` (full upgrade/check/downgrade cycle).
- [x] 6.3 Add `create_knowledge_base` (`control_plane_backend/knowledge_base/service.py`), failing closed on an unregistered/disabled knowledge base type, then on a team lacking `can_use`. Verify: `tests/test_knowledge_base_service.py` (4 tests).

## 7. `DocumentIngestionPort` (`fred-sdk`) (design.md D8)

- [ ] 7.1 Add `DocumentIngestionPort` to `libs/fred-sdk/fred_sdk/contracts/runtime.py`, sibling to `DocumentContentPort`/`DocumentSearchPort`/etc.: `async def submit(item: SourceItem, artifact_path: Path, knowledge_base_id: str) -> str` and `async def delete(document_id: str, *, knowledge_base_id: str) -> None`, raising the existing `DocumentPortCallError` on transport failure (no new exception type). No user/team identity parameter — matches the existing family's convention that the adapter's own binding supplies identity. **Corrected (design.md D16, 2026-09-07 review pass): `knowledge_base_id` on `submit`/`delete` is defense-in-depth, not the authorization.** The real guarantee comes from construction: the execution host builds one adapter instance per sync cycle, only after authorization passes for one specific `knowledge_base_id` (design.md D17), and that adapter privately captures its `knowledge_base_id` — the explicit argument must match the adapter's own bound scope or the call is rejected, and separately the resolved document's own recorded knowledge base must match before a delete proceeds. `submit` MUST be idempotent per `(knowledge_base_id, item.source_item_id, item.revision)` (D8/D14). Document both the binding requirement and the idempotency requirement on the ABC's docstring, not just in design.md. Verify: a test proving a minimal fake/in-memory implementation satisfies the ABC (same style as `SourceConnector`'s own protocol-satisfaction test in `libs/fred-sdk/tests/test_knowledge_base_contracts.py`); a test asserting a fake adapter constructed bound to one `knowledge_base_id` rejects a `submit`/`delete` call whose explicit `knowledge_base_id` argument disagrees with its own binding, not just a document-ownership mismatch.

## 8. Local adapter, and both MVP knowledge base types retrofitted onto it (`knowledge-flow-backend`) (design.md D9)

- [ ] 8.1 Implement a local `DocumentIngestionPort` adapter inside `knowledge-flow-backend`, calling `extract_metadata`/`save_input`, `push_input_process`, `output_process`, `delete_document_and_artifacts` exactly as `sync_pull_knowledge_base` already does today — the adapter is this same logic moved behind the port interface, not new ingestion behavior. **Ordering correction (design.md D15, 2026-09-07): on a changed-content update, ingest and durably confirm the replacement BEFORE deleting the prior version** — the merged `sync_pull_knowledge_base` currently deletes first, which loses the item entirely on a failed re-ingest; the adapter must not repeat that ordering. Verify: a test exercising the adapter directly against the same mocked-ingestion-primitives convention `tests/features/scheduler/test_pull_knowledge_base_sync.py` already uses, including a case where ingestion of the replacement fails and asserts the prior version's document/artifacts were never deleted.
- [ ] 8.2 Refactor `sync_pull_knowledge_base` to call the Task 8.1 adapter instead of importing `ingestion_service`/`push_files_activities`/`activities`/`MetadataService` directly. Verify: `tests/features/scheduler/test_pull_knowledge_base_sync.py` still passes, now mocking the adapter's port methods instead of the four underlying functions individually.
- [ ] 8.3 Refactor the existing manual-upload path (`knowledge_flow_backend/features/ingestion/ingestion_controller.py`) to call the *same* Task 8.1 adapter for document creation, instead of calling `push_input_process`/`output_process` directly as it does today — this is the retrofit the developer required: the existing `rag_sql`/push knowledge base proven to share the identical ingestion hand-off pull now uses. Verify: the existing upload-path test suite (`tests/features/ingestion/` and whatever covers `ingestion_controller.py` today) still passes unmodified in behavior, only in call site.

## 9. Wire contract for a future remote adapter — design artifact only, no remote adapter built (design.md D10)

- [ ] 9.1 Write the `DocumentIngestionPort` remote wire contract (request/response shape for `submit`/`delete` over M2M-authenticated HTTP: `SourceItem` fields, a knowledge-base/team-scoped authorized upload target, the artifact transfer shape; least-privilege/short-lived artifact access, never raw credentials) as a fixture or a short doc alongside the port definition — not an implemented endpoint, not a remote adapter class. Verify: the fixture/doc exists and is referenced from `DocumentIngestionPort`'s own docstring, so a future remote adapter has a named starting point instead of re-deriving the shape from scratch.
- [ ] 9.2 Portability proof (design.md D19, corrected 2026-09-07 review pass — must exercise the knowledge base implementation, not `DocumentIngestionPort` in isolation): write one shared contract-test suite that runs a real `LocalFilesystemConnector` against a real temporary directory through discover→fetch→submit→query and discover→fetch→delete, asserting on real, queryable document outcomes (not a fake satisfying the ABC). Run it twice: (1) in-process, connector and Task 8.1's local adapter in the same process as the test; (2) with the same connector instance running in a **separate OS subprocess** the test spawns, handing fetched bytes to the Task 8.1 adapter over a loopback `httpx` call (D10's wire shape) rather than a local function call. No Kubernetes, no second deployable service. Verify: both runs produce identical, real, queryable documents from the identical test fixture directory; only the connector's process boundary differs between them.

## 10. M2M read path and `knowledge-flow-backend` consumption of `KnowledgeBase`/`KnowledgeBaseType` config (design.md D7)

- [ ] 10.1 Add a minimal M2M read endpoint on `control-plane-backend` returning one `KnowledgeBase` plus its `KnowledgeBaseType` by `knowledge_base_id`. **Corrected (design.md D17, 2026-09-07 review pass): the authentication mechanism is not an open question — reuse `fred_core.security.backend_to_backend_auth` (`M2MAuthConfig`/`M2MTokenProvider`/`M2MBearerAuth`)**, the same machinery control-plane's own `get_service_token_provider()` (`app/context.py`) and knowledge-flow-backend's own `_init_m2m_client()` (`application_state.py`) already build on, extended with a real network `httpx.AsyncClient` from `knowledge-flow-backend` to control-plane using KF's own `M2MTokenProvider`. **What is actually open, and must be resolved with the developer before implementing this task's authorization check: `is_service_agent(user)` alone is too broad** (any backend's service-agent-role principal, not specifically `knowledge-flow-backend`'s) — decide the narrowing (caller's Keycloak `client_id`/`azp` claim allow-listed, or a scoped permission) before writing the endpoint's dependency. Verify: a contract test exercising the endpoint with `knowledge-flow-backend`'s own M2M identity, and a denial test for a *different* service-agent-role caller (not just an unauthenticated one) to prove the narrowing actually discriminates. Also re-check `knowledge_base_type#can_use` for the knowledge base's team on this read (design.md D17b) so a revoked team's next sync cycle observes the revocation, not just at creation.
- [ ] 10.2 Add a small `knowledge_base_sync_state` table in `knowledge-flow-backend` (`knowledge_base_id`, `state`, `updated_at`), next to `kf_task_run`/`kf_task_event_log` — this table, not control-plane, owns the sync cursor/document-id map (design.md D7). **Persist per item, not per batch (design.md D14, 2026-09-07): write this row after each discovered change's `DocumentIngestionPort` call durably lands, not once after the whole `discover_changes()` batch finishes** — the current merged `sync_pull_knowledge_base` only persists once per batch, which loses cursor progress (and, absent Task 7.1's idempotent `submit`, would duplicate documents) on a mid-batch crash. Verify: a store-level test with a real SQLite engine, mirroring `tests/features/test_task_store.py`'s pattern; an Alembic migration passing `make db-check-heads`/`make db-check-sqlite` in `knowledge-flow-backend`; a test asserting a crash after item N but before item N+1 leaves state reflecting exactly N processed items.
- [ ] 10.3 Update `sync_pull_knowledge_base` to fetch `KnowledgeBase`/`KnowledgeBaseType` via the Task 10.1 endpoint once per sync cycle (not per file) instead of receiving them as plain arguments, and to read/write its state through the Task 10.2 store (per-item, per above) — on top of the Task 8.2 refactor, not instead of it; `sync_pull_knowledge_base` by this point calls the ingestion port (Group 8) *and* reads its config/state this way. Record the run as one `kf_task_run`/`kf_task_event_log` task, kind `knowledge_base_sync` (design.md D12) — accepted/completed map to that task's own state, never a separate status field. **No secret in any emitted event (design.md D18, spec.md "no resolved credential or secret" requirement)**: `TaskEvent.detail`/`error`/`target` for this kind must carry only knowledge-base/item-identifying data, never a resolved `connector_ref` or credential. Verify: `tests/features/scheduler/test_pull_knowledge_base_sync.py` updated and passing with the M2M call and the new state store both exercised (mocked at the boundary, per this file's existing test convention); a test asserting no emitted `TaskEvent.detail` contains `connector_ref` or any credential-shaped value.

## 11. Validate the no-overlapping-synchronization requirement end to end

**Blocked on design.md's Open Questions (D9/D13, 2026-09-07 review pass) — whether `knowledge_base_sync` reuses `IngestionTaskService`'s `SchedulerBackend` (Temporal-capable) or stays a bespoke `MEMORY`-only island. Do not start 11.1-11.3 until that is decided**, because it determines whether Task 11.2's liveness check already exists (Temporal) or must be built (in-process).

- [ ] 11.1 Add a partial unique index on `kf_task_run`, scoped to `kind='knowledge_base_sync'` and keyed on `target->>'id'` where `target->>'type'='knowledge_base'` among non-terminal states (corrected 2026-09-07: `TaskTarget` is `{type, id, label}` — there is no `knowledge_base_id` field; do not add one, reuse the existing schema) — the same mechanism `single_active_migration_index_name`/`task_run_table_args` already uses for `kind='migration'`. A second `TaskService.start()` for a knowledge base already non-terminal MUST raise the resulting `IntegrityError`, translated to 409 the same way `import_export/api.py::_is_concurrent_migration_violation` does today. **This closes replayed-submission dedup only — it does not by itself guarantee a single worker executes the accepted task; see 11.2/11.3.** Verify: a test that starts two synchronizations for the same knowledge base instance concurrently and asserts the second's `start()` is rejected with 409 while the first is in progress (specs/knowledge-base/spec.md — "A knowledge base instance never runs two overlapping synchronizations" and "A replayed synchronization submission never creates a duplicate run"); a second test asserting a synchronization requested after the prior one reached a terminal state IS accepted.
- [ ] 11.2 Bind acquisition: after 11.1's `start()` succeeds, call `TaskService.bind_execution(task_id, execution_id=...)` (design.md D13) with a real, checkable `execution_id` — a Temporal workflow id if Group 11's blocking decision above went that way, or a purpose-built in-process liveness handle otherwise (a heartbeat the execution host renews by calling `record_event` faster than a grace period, with a `WorkflowControl` implementation that answers `reconcile_stale`'s status check honestly — `fred_core.tasks`' own `_reconciled_terminal` refuses to declare a task terminal from staleness alone, so this must provide a real authoritative verdict, not just an age check). Verify: a test that a crashed (never-completed) synchronization's task_run is eventually reconciled to `failed` by `reconcile_stale`, and that a new synchronization for that knowledge base is then accepted (11.1's index no longer blocks it); a test that a synchronization still genuinely running is never reconciled terminal regardless of grace-period elapsed time.
- [ ] 11.3 Fencing: the execution host re-checks, immediately before every `submit`/`delete` call (not only once at sync start), that its own `task_id` is still non-terminal and still bound to its own `execution_id` — mirroring `_emit_terminal_if_diverged`'s own "re-fetch for a fresh terminal check... never overwrite a terminal state" discipline, applied before a mutating I/O call rather than before a DB write (design.md D13). Verify: a test simulating a worker reconciled `failed` mid-batch (another process/thread drives its task terminal underneath it) and asserting the original worker's next mutating call is refused rather than proceeding — matching specs/knowledge-base/spec.md's "A worker no longer recognized as the active executor... cannot complete further mutations" requirement.
