## 1. Predecessor alignment and manifest authority

- [x] 1.1 Confirm the synchronized main, active predecessor task 12.7, and implemented retirement delta; document predecessor → retirement → this-change sync/archive order and verify no incident requirement is restored in a disposable semantic sync.
- [x] 1.2 Add one schema-checked registered-package inventory for the three contained producer members, with stable IDs and explicit build/validator/consumer profiles; verify unknown, duplicate, root, and escaping registrations fail in controlled tests.
- [x] 1.3 Replace the three-required-role and copied `expectedManifest` fields in the live release-contract/schema/parser with inventory-driven manifest reads; verify first-release alpha.1 coordinates remain historical evidence, a reviewed future SDK-only version is accepted without changing UI/tokens, and mismatched name/version/export fails.
- [x] 1.4 Introduce one reviewed release-policy input/schema for scope, repository, branch, registry, workflow filename, environment, access/tag, provenance issuer, owners, and exact producer toolchain; verify conflicting manifest or archive metadata fails before packing.
- [x] 1.5 Add per-member changelog/version review checks without automatic bumping; verify a selected matching entry is accepted and missing/mismatched/unreviewed entries block publication in controlled tests.
- [x] 1.6 Preserve the private producer root, declared contained npm workspace links, and separate application pins while removing duplicated manifest copies; verify producer-lockfile boundary and exact Node/npm tests, including rejected unexpected links and published local references.
- [x] 1.7 Update fixture contracts/tests to the inventory/policy model with an explicit nonapproved state; verify fixture/proposed evidence cannot authorize genuine publication or registry success and existing development commands remain green.

## 2. Selection and compatible dependency baselines

- [ ] 2.1 Implement duplicate-free selected-ID parsing shared by CLI/workflow, with deterministic ordering; verify empty, duplicate, unknown, and private-root selections fail before pack or publish.
- [x] 2.2 Parameterize the existing packers/candidate validators to produce release candidates only for selected members while retaining package-specific archive/license/asset/reference checks and separate producer-wide regressions; verify a future SDK-only version creates no token/UI candidates, plus token-only, UI-only, combined sets and existing token/UI/SDK negative suites.
- [x] 2.3 Add a self-contained source-reviewed compatibility ledger entry for alpha.1 tokens after one-time cross-check of historical evidence against exact registry bytes and cryptographic provenance; verify exact ID/name/version/registry/SHA-512, expected digest/repository/publication commit/workflow/signer/issuer, and verification trace are durable without modifying historical records or requiring the old CI ZIP later.
- [ ] 2.4 Reject missing, altered, invented, or unverified durable token baseline expectations, while allowing UI-only validation after the original token CI ZIP expires; verify exact-registry byte/provenance positives and negatives without tags, workspace sources, or candidate-publication retry from an expired artifact.
- [ ] 2.5 Resolve UI's actual committed token peer range against selected token bytes or the approved exact prior baseline; verify UI-only passes with compatible tokens, incompatible/unapproved baselines fail, and combined tokens publish before UI.
- [ ] 2.6 Keep SDK selection independent of token/UI while separating selected publication members from compatibility-only installs; verify SDK-only neutral consumer, cross-origin browser, host regressions, and no UI/token publish command or UI browser prerequisite.
- [x] 2.7 Preserve source-isolated offline selected-candidate `file:` references and exact compatibility-only registry cache resolutions as distinct from strict registry-only verification; verify allowed integrity-checked tarballs, UI-only cached token installation after old ZIP expiry, and rejection of directories, workspace/checkout fallback, or reused FRED trees.

## 3. Generated candidate record and immutable transfer

- [x] 3.1 Add an inventory-driven release-record schema/generator recording reviewed source commit, selected/compatibility coordinates and manifest ranges, durable baseline digest, policy digest, expected provenance, observed producer/application Node/npm, gate results, and actual selected filenames/lengths/SHA-512; verify exact fields and no hard-coded three-role requirement in unit tests.
- [x] 3.2 Reuse candidate/archive validation and generate the record from the same packed bytes; verify altered archive bytes, missing license/asset/export, wrong coordinate, and stale/incomplete/fixture records fail before approval.
- [ ] 3.3 Parameterize immutable evidence/fixture-transfer and application-toolchain transfer for selected archives; verify original record digest, GitHub origin run/attempt, artifact ID/API identity, ZIP SHA-256, and archive integrities before/after compatibility without rebuilding.
- [ ] 3.4 Add a shared pre-publication attempt record binding original candidate digest/selected coordinates/bytes and independent actual repository/workflow/commit/run/attempt/signer/issuer; upload it as a separate retained GitHub artifact and read back API identity, ZIP and record digests before any publish command; verify failed upload/readback prevents publication and no credential enters the artifact.
- [ ] 3.5 Add separate bound per-package verified publication outcomes and verifier identities without mutating candidate or attempt fields; verify a missing final outcome can be recovered only from the retained attempt plus cryptographically matching registry evidence, different execution identities stay truthful, and spoofed/mismatched identity fails.
- [ ] 3.6 Make original candidate and attempt artifact selection explicit across new dispatches/failed-job reruns instead of deriving it from current `run_attempt`; verify attempt-two retrieval of pinned earlier artifacts and rejection of another ID/digest or expired selected-publication artifact.

## 4. Ordinary workflow and direct publishing boundary

- [ ] 4.1 Extend the existing `.github/workflows/Publish-frontend-packages.yml` with validated `prepare-only`, `publish`, and `verify` manual inputs, preserving the filename, `workflow_dispatch`, committed-`swift` guard, and default preparation; verify YAML/workflow-contract tests reject push publication and obsolete modes.
- [ ] 4.2 Reuse current producer/application preparation and provisioning under separate toolchains for selected packages; verify a `prepare-only` workflow-contract simulation schedules candidate and compatibility jobs but no environment approval, registry mutation, publishing credential, or `id-token: write`.
- [ ] 4.3 Add one protected `npm-publish` publication job using the exact transferred tarballs and direct GitHub-hosted npm OIDC, with `id-token: write` limited to that job and no bootstrap secret; verify prepare/verify/PR jobs cannot schedule or read publishing authority.
- [ ] 4.4 Define maintainer review of each existing package's exact npm Trusted Publisher organization/repository/workflow filename/environment/direct-publish permission as a prerequisite; validate only repository policy/current execution in CI, and verify a real npm authorization rejection stops without token/interactive-login/custom-auth fallback or `npm whoami`/dry-run success claims.
- [ ] 4.5 Check record/source/policy/manifests, reverify original archive bytes, and prove durable attempt-artifact upload/readback *before* each publish command; verify stale source, fixture evidence, missing/changed candidate or attempt artifact, failed attempt persistence, or unapproved version/changelog prevents publication.
- [ ] 4.6 Implement selected dependency publication ordering and immediate exact-version identity/integrity/provenance verification; verify selected tokens complete before UI and a failed token check prevents UI publication.
- [ ] 4.7 Add a read-only `verify` operation that retrieves explicit retained evidence and schedules only provisioning, exact registry verification, clean consumers/browser/host as applicable; verify no pack, publish, `npm-publish` approval, bootstrap secret, or OIDC write path is reachable.

## 5. Read-only reconciliation and public-registry guarantees

- [ ] 5.1 Parameterize exact-version preflight into absent/matching/unsafe states with approved name/version/SHA-512 and cryptographic provenance comparisons bound to a verified outcome or pre-command attempt; verify malformed, unauthorized, wrong-byte or wrong-actual-publishing-identity versions fail before another publish command.
- [ ] 5.2 Add bounded temporary-404 read-only exact-version/package-wide visibility retries, with immediate auth/malformed/integrity/identity failures; verify initial 404 then valid, exhausted 404, and mismatch regressions without automatic republishing.
- [ ] 5.3 Implement explicit partial-release continuation from original candidate plus prior outcomes **or durable attempts when outcomes were lost**, requiring fresh protected approval/new persisted attempt and publishing only demonstrably absent exact versions; verify matching bytes/provenance are skipped, unsafe/missing attempt evidence stops progression, and no GitHub identity is spoofed.
- [ ] 5.4 Add controlled npm-accepted-then-job-failed regression: omit final outcome after a retained attempt, reconcile against actual registry bytes/cryptographic provenance, skip the matching version and continue only missing versions; verify a mere attempt or ambiguous response never claims success or automatically reruns publish.
- [ ] 5.5 Make independent verification repeatable against the same retained candidate/attempt records even when all selected packages exist; verify current verifier commit/run/attempt differs from historical publishing identities and changed/missing selected candidate or attempt bytes fail.
- [ ] 5.6 Parameterize `registry-verifier.mjs` for selected packages plus approved durable exact compatibility-only dependencies; verify UI-only succeeds after old token ZIP expiry, no local tarball/workspace/checkout/tag fallback, and installed nonlinked tree precedes mandatory `npm audit signatures`.
- [ ] 5.7 Retain mandatory downloaded SHA-512, npm signatures, Sigstore certificate verification and independent expected digest/repository/actual publication commit/workflow/issuer checks from outcome, attempt, or durable baseline; verify a validly signed wrong release is rejected for each identity field.
- [ ] 5.8 Run selected clean registry consumers, browser smoke, and production-host compatibility with network-capable cache/Chromium provisioning separate from offline installation/build/execution; verify SDK-only and UI-only-after-old-ZIP-expiry matrices, local asset requests, and actionable missing-prerequisite failures.

## 6. Extensibility, CI, and documentation

- [x] 6.1 Add a disposable fourth-package registration/profile fixture without creating or publishing a real package; verify selection, record schema and generic verifier accept it while missing specialized validator/consumer/creation/trust authorization blocks release.
- [x] 6.2 Extend `package-inputs.mjs`, CI workflow selection, and workflow-contract tests for inventory/policy/changelogs/record/compatibility inputs, member manifests/lockfiles, canonical CSS/components/protocol, assets/notices, React and host inputs; verify positive affected-input and unrelated-application skip scenarios.
- [ ] 6.3 Preserve per-job consumer cache/Chromium provisioning before offline tests and exact producer/application toolchain pins; verify CI-contract regressions fail if selected jobs depend on another job's filesystem or download during validation.
- [ ] 6.4 Document reviewed manual version/changelog PR, package selection, candidate plus pre-command attempt artifacts, lost-outcome reconciliation, durable prior-token baseline versus expiring retry archives, one approval, actual retention, maintainer-reviewed direct trust versus stage permission, authorization failure/no credential fallback, fourth-package creation, and rollback in `libs/frontend/RELEASE.md`; verify links/commands against implemented CLI and preserve historical appendix unchanged.
- [ ] 6.5 Update producer/consumer docs and only the RFC's release sequencing statements affected by routine publication; verify no duplicate RFC, no FRED/RAGS adoption claim, and all relative repository links resolve.

## 7. Integration evidence and close-out readiness

- [ ] 7.1 Run controlled release-contract/selection/record/publisher/verifier tests under Node 24.21.0/npm 11.19.0; record exact commands and results, clearly distinguishing fixtures from genuine public-registry/OIDC evidence.
- [x] 7.2 Run producer lint/format/quality/unit, all existing token/UI/SDK positive and negative archive checks, offline isolated consumers, and browser smoke with separate provisioning; record exact commands/results and no weakened acceptance cases.
- [x] 7.3 Run affected FRED application quality, production build, host/proxy/authentication/iframe regressions under its separate Node 22.13.0/npm 10.9.2 toolchain; record exact commands/results without migrating application sources.
- [x] 7.4 Obtain the repository-required independent implementation review and resolve in-scope findings; verify review evidence and resolution are documented without marking an execution-dependent real publish successful from fixture tests.
- [x] 7.5 Run `openspec validate add-frontend-independent-releases --strict`, a disposable retirement-then-corrected-delta semantic merge/main-spec validation, and `git diff --check`; verify all seven modified main requirements retain unaffected scenarios, future manifest versions/selected candidates/actual publishing provenance do not contradict historical alpha.1 facts, and no obsolete operation returns.
- [ ] 7.6 Before any later sync/archive, verify predecessor task 12.7 has its actual trust/revocation evidence or remains explicitly open, settle predecessor/retirement close-out in order, then use `openspec-sync-specs` to compare and incorporate this implemented delta without losing unaffected scenarios; verify resulting main-spec strict validation before archival.

## Migration-plan step 1 evidence (uncommitted; 2026-09-15)

- Tasks 1.1–1.7, 2.3, 3.1, 6.2, 7.2, and 7.4 have their bounded implementation and test evidence. The current manifests, versions, and public exports remain alpha.1; an SDK alpha.2 was exercised only in a disposable producer fixture. Task 2.4 has only its durable-ledger/no-historical-ZIP groundwork: selected UI-only registry/cache and consumer behavior remains for later tasks 2.4, 2.5, and 2.7. Tasks 3.2–3.6 still require selected-only packing, immutable attempt upload/readback, and genuine publication/verification linkage. Task 7.1 retains its phase-1 release-contract/record/legacy-verifier test evidence but remains unchecked until the selected-ID parser and ordinary publisher tests exist. The modeled attempt/outcome/verification records are nonauthorizing scaffolding, not completed task 3.4 or public-registry evidence.
- Independently fetched GitHub artifact `10365859307` (verification run `34890367123`, attempt `1`): API name/digest matched the ledger; ZIP SHA-256 `9d3ba17c4016fd194c64527cb10f95d6c711e5963a37b7cf95386d23620981b4` and embedded `final-evidence.json` SHA-256 `aabbde2d1e1bf82cbb93d92a27a59a97a291ae3ee5915f51f49e46776f586c4f` matched independent trace fields. Exact npm metadata and downloaded tarball for `@fred-oss/design-tokens@0.1.0-alpha.1` matched SHA-512 `sha512-+3UeYRe4Qhgtx+U1T/QQqu9172N+7c+DbuSo8G/2mvp5nbrjgLJj1VDdcOv4AYsuaYEvpNzERHo7Z6GTFNenqw==`. The default Sigstore verifier (not a test stub) validated the attestation and independently expected digest, repository, original publication commit `f49f2439d54b44f7739c5bd7fca3f789e0e528d6`, workflow, and issuer. `verifyBaselineImport` returned `cryptographicallyVerified: true`. Later ledger loading and controlled exact-byte/identity verification succeeded after the fixture historical ZIP was deleted; live UI-only resolution without that ZIP is deliberately not claimed.
- Producer Node `24.21.0`/npm `11.19.0`: `npm run release:check` passed; `npm run release:test` passed 100/100 controlled tests (a sandbox-only localhost `EPERM` required the approved unsandboxed rerun); `make code-quality` passed ESLint/Prettier; `make test` passed 304/304; `make pack-check` passed token/UI/SDK archive validators; `make isolated-consumer` passed all three offline tarball consumers; `make browser-smoke` passed light/dark, local asset, font, iframe and SDK browser checks. Consumer dependencies were separately provisioned by `make consumer-provision`; Chromium by `make browser-install` before offline validation. Application Node `22.13.0`/npm `10.9.2`: `make host-integration` passed the packed SDK host test (4/4); the separate-directory `fixture:transfer:validate` passed consumers/browser/host and emitted fixture `final-evidence.json` plus `candidate-record.json` bound to the verified producer transfer run/attempt and metadata digest. The corresponding `fixture:transfer:create` ran under producer pins and transferred exactly copied archives; this rehearsal remains a fixture, not approved release evidence.
- Independent cold review reproduced four initial gaps: misbound profile tuples, private member admission, forged complete candidates, and incomplete compatibility-only fields; all were corrected with negative tests. Follow-up review found a two-file receiver-persistence failure and missing transfer-origin binding; paired-write failure cleanup, an injected second-rename regression, and candidate `transferOrigin` fields/tests resolved them. Final review caught an approved-transfer helper that admitted injectable fixture packers/disposable source roots: the transfer constructor is now private and the approved wrapper requires canonical reviewed policy/manifests, canonical producer root, clean-source input, and actual packers; disposable tests reject these substitutions while the positive path uses real packers. Task 7.1 was also returned to unchecked because ordinary publisher/selected-ID tests are later slices. No publication or GitHub workflow execution occurred.
- Disposable composition applied implemented `retire-frontend-bootstrap-recovery` then this delta to a copied OpenSpec tree only: original main contained 36 requirements; resulting main contained 46, with all 8 added and 7 modified delta requirements/scenarios incorporated, every unaffected earlier scenario retained, and both retired incident requirements absent. `openspec validate --specs --strict` passed before and after the simulated second sync. This is not synchronization or archival in the repository. Predecessor task 12.7 remains unchecked for missing trust/revocation evidence; its active change must not be marked complete by this slice.
- In the real checkout, `openspec validate add-frontend-independent-releases --strict` passed with the expected informational warning that the retirement-created requirement is not yet in unsynchronized main; the disposable post-retirement semantic composition resolves that header. `openspec validate --all --strict` passed 12/12 items. `git diff --check` passed. Neither main specs nor active changes were synchronized or archived.

## Migration-plan step 2 evidence (uncommitted; 2026-09-15)

- Newly completed bounded tasks: 2.2, 2.7, 3.2, 6.1, and 7.3. The checklist is 18/46 complete. Task 2.1 has shared CLI parsing but **not workflow parsing**; 2.4 has reviewed exact baseline provisioning and UI-only offline validation without the old CI ZIP but **not selected-candidate artifact expiry/publication-retry guards**; 2.5 and 2.6 have selected dependency/SDK behavior but **not publication commands**; 3.3 has selected immutable local producer/application transfer but **not GitHub artifact API/readback binding**; 5.6–5.8 have selected controlled registry-helper/consumer/browser/host coverage but **not durable outcome/attempt-bound genuine registry success**. Those boxes and ordinary workflow/publishing tasks remain unchecked for slice 3. Task 7.1 also remains unchecked because ordinary publisher tests do not yet exist. Predecessor task 12.7 remains open for its actual trust/revocation evidence.
- Selection is inventory-driven and topologically ordered even when omitted. `--select` rejects empty, duplicate, unregistered, and private-root IDs. `make -n RELEASE_SELECTION= release-transfer-create` retains `--select ""` rather than silently selecting all. An SDK-only future coordinate is exercised in a disposable fixture without changing committed member versions; token-only, UI-only, and token+UI candidate generation retain actual member-specific archive validation. The disposable fourth-package registration validates generic selection/record/registry identity but cannot enter the canonical producer or gain specialized archive, consumer, creation, or trust authority.
- UI-only preparation independently fetched exact `@fred-oss/design-tokens@0.1.0-alpha.1` registry bytes and repeated npm installed-tree signature auditing plus cryptographic Sigstore provenance comparison with the committed ledger's expected integrity, repository, original publication commit, workflow, and issuer. `npm run release:provision:ui-baseline` passed using Node 24.21.0/npm 11.19.0; the token archive and receipt entered a dedicated prepared cache. Negative controlled tests reject wrong exact-version metadata, integrity, validly signed but wrong repository, tampered receipt/archive, missing baseline, and incompatible UI peer range before cache warming or offline validation. The original token CI ZIP was neither downloaded nor consulted. A disposable current-token-version divergence (`0.1.1-alpha.1` registered versus reviewed `0.1.0-alpha.1` baseline) passed offline neutral/React consumers against the older exact baseline; it is not a future approved coordinate.
- The selected fixture transfer used real packers once under Node 24.21.0/npm 11.19.0 and `fixture:transfer:create --select iframeSdk`, `--select designTokens`, `--select ui`, and `--select ui,designTokens`. Exact archives/evidence were copied to separate receiver directories; `fixture:transfer:validate` under Node 22.13.0/npm 10.9.2 checked metadata, lengths, SHA-512, selected-only records, offline consumers, applicable Playwright browser smoke, and SDK production-host compatibility without receiver rebuilding. All four transfers passed with fixture readiness, not approved candidate/public-registry evidence. UI-only installed its selected UI `file:` tarball plus exact cached token registry version into a source-isolated React consumer with zero package links/checkout references; SDK-only used its neutral consumer, distinct-origin browser, and host test without token/UI prerequisites. Combined token/UI checked the committed peer range and dependency order.
- Producer Node 24.21.0/npm 11.19.0: `make release-check release-test code-quality` passed 114/114 controlled release tests, ESLint, and Prettier; `make test pack-check isolated-consumer browser-smoke` passed all producer, actual token/UI/SDK archive positive/negative, offline consumer, and browser gates; a final `npm test` passed 320/320. Focused `node --test tests/release-selection.test.mjs` passed 5/5. `make consumer-provision` and `make browser-install` separately refreshed the pinned caches/Chromium. Refreshing the React cache *after* a baseline provision intentionally made UI-only `npm --offline` fail with an actionable `only-if-cached` prerequisite error; `make compatibility-provision` afterward repeated exact registry/Sigstore checks, and `npm run test:consumer:ui -- --compatible-token`, `npm run test:consumer:tokens -- --compatible-token`, and `npm run test:consumer:iframe-sdk` all passed offline. Selected UI/SDK `PLAYWRIGHT_BROWSERS_PATH=target/playwright npm run test:browser -- --select <id>` passed with local-only requests and no installation during execution. Application Node 22.13.0/npm 10.9.2: `npm run typecheck && npm run format && npm run lint && npm run build && npm test` passed, including proxy smoke (9/9) and Vitest 225 files passed, 2 skipped; 2375 tests passed, 6 skipped. `npm run test:host-integration` passed 4/4 with the packed SDK. No application source or public API changed.
- Independent cold review found five in-scope source/command gaps and all were resolved: independently versioned current token manifests had to resolve the prior baseline coordinate; omitted selection needed topological ordering; direct records needed exact selected/compatibility binding; browser check names needed an allowlist; and the Make wrapper had to distinguish explicit empty selection and use the provisioned Chromium path. Positive and negative regressions were added; final source/test review reported no remaining in-scope defect. A checklist-honesty follow-up returned task 2.4 to unchecked because selected-candidate expiry/publication-retry protection belongs to slice 3; its baseline/consumer evidence is recorded as partial progress. Unbound attempt/outcome identities and ordinary workflow dispatch remain intentional slice-3 limitations.
- `openspec validate add-frontend-independent-releases --strict` passed with the expected informational unsynchronized-retirement-header warning; `openspec validate --all --strict` passed 12/12. In a disposable copied OpenSpec tree only, retirement then this delta semantically composed to 46 requirements from 36 original: retirement removed 2/added 4; this delta added 8/modified 7; no old unaffected requirement/scenario or delta scenario was lost and neither retired incident requirement returned. `openspec validate --specs --strict` passed after each simulated merge. `git diff --check` passed. Real main specs and active changes were not synchronized or archived.

## Slice 2 transfer-schema correction evidence (uncommitted; 2026-09-15)

- Reviewed follow-up commit `f8a1062907083a66bcef3f3b82742cd06fbfc98d` started clean at `feat/frontend-selected-package-validation`; four stashes remain untouched. A newly generated actual UI-only `fixture:transfer:create --select ui` produced `selectedIds: ["ui"]` and only the UI package. The pre-fix direct `assertDocumentSchema` failed because the old schema rejected `selectedIds` as an additional property and required token/SDK records. The shared transfer validator had not applied that schema, so its generated output and the committed schema could diverge.
- The existing `release/fixture-transfer.schema.json` now checks a nonempty duplicate-free selection and count-agnostic package-record map with inventory-ID syntax, while the existing shared metadata validator applies the schema and retains semantic registration, canonical ordering, exact selected package set, contract/source/execution identity, toolchain, canonical filename/length, and SHA-512 checks. A missing `selectedIds` is accepted only for a complete all-member transfer; omitting it from UI-only metadata fails the package-set check. The schema shape can admit a future syntactically valid ID, but the current inventory validator rejects an unregistered one.
- `node --test tests/fixture-transfer.test.mjs` passed 31/31 with generated default all-member, SDK-only, tokens-only, UI-only, and token+UI metadata; legacy all-member omission through the complete receiver fixture-evidence path; empty, duplicate, unknown, private-root, and unordered selections; missing/extra package records; and the existing identity/integrity/file-set negatives. The direct schema check of the actual UI-only transfer changed from the reproduced failure to success. This correction strengthens completed task 2.2 and partial task 3.3 evidence without checking another task or claiming GitHub artifact readback/publication. The checklist remains 18/46 complete; predecessor 12.7 remains open.
- Under producer Node 24.21.0/npm 11.19.0, `make code-quality test release-check release-test pack-check` passed ESLint/Prettier, full unit/release tests, release contract checks, and token/UI/SDK archive checks. Separate final `npm test` passed 327/327 and `npm run release:test` passed 121/121 controlled tests. Five real-packer `npm run fixture:transfer:create` commands (default, `--select iframeSdk`, `designTokens`, `ui`, `ui,designTokens`) passed with exact generated package sets and automatic shared schema/semantic validation. These outputs remain fixture rehearsals. `openspec validate add-frontend-independent-releases --strict` and `openspec validate --all --strict` passed with the same expected informational unsynchronized-retirement-header warning; `git diff --check` passed. Independent cold review found no remaining in-scope schema/semantic or checklist defect. No workflow dispatch or publication ran.
