## 1. Reconcile evidence and trace dependencies

- [x] 1.1 Recheck branch, HEAD, status, four preserved stashes, applicable instructions, active predecessor change, main specification, RFC, and #2630; record the read-only baseline and verify no unrelated work is altered.
- [x] 1.2 Recheck run `34890367123` attempt 1 and artifact `10365859307` against GitHub metadata and ZIP SHA-256; compare `final-evidence.json` coordinates, SHA-512, original versus later publication commits, verifier execution, and all six successful gates; add a compact immutable historical record to the existing runbook with exact run/artifact links and expiry, without replacing source evidence or claiming trust setup/token revocation.
- [x] 1.3 Map every live import/read/command/test-loader/workflow caller of both incident JSON files and five exclusive modules; verify an `rg` inventory and predecessor task 12.7 remain explicit before planning deletions. Verify the already-synchronized predecessor requirement/scenario inventory and later predecessor-first archival order without marking missing npm evidence complete.

## 2. Retain preparation, remove privileged workflow paths

- [x] 2.1 Reduce `.github/workflows/Publish-frontend-packages.yml` to its existing `swift`-only preparation dispatch and candidate/application compatibility jobs; remove three retired choices and all exclusive publication/recovery/verification jobs, `needs`, conditions, pinned incident downloads, environment, bootstrap secret, and OIDC write permission; verify workflow-contract tests prove no input can schedule registry mutation and the default preparation path still schedules both toolchains.
- [x] 2.2 Update workflow/CI selection contract tests for preparation-only operation, preserved immutable archive transfer, separate consumer/browser provisioning, and every still-consumed canonical/release input; verify focused `ci-selection.test.mjs` and its deletion-selection scenario pass.

## 3. Retire incident scripts and configuration without weakening verification

- [x] 3.1 Move or re-express bounded read-only exact-version reconciliation from `bootstrap-publish.mjs` in the existing `registry-metadata.mjs` only if generic callers/tests still need it; verify positive delayed-404 and negative malformed/unauthorized/identity/integrity cases in `registry-metadata.test.mjs` before deleting the publisher.
- [x] 3.2 Remove the five exclusive production modules `bootstrap-publish.mjs`, `bootstrap-recovery.mjs`, `bootstrap-recovery-contract.mjs`, `recovery-artifact.mjs`, and `registry-verification-continuation.mjs` after final import audit; verify no live module imports them, generic verifier loads in a fresh bounded Node process, and package quality tests pass.
- [x] 3.3 Remove recovery/continuation imports and CLI branches from `registry-verifier.mjs`; explicitly reject retired `--recovery-*` and `--verification-*` flags without ignoring them; verify fresh-process negative CLI tests plus retained exact coordinate, SHA-512, installed-tree signature, Sigstore, independent provenance, dependency-boundary, read-only visibility, and clean-consumer positive/negative tests.
- [x] 3.4 Remove the unconditional `bootstrap-recovery.json` read/validation in `check-release-contract.mjs`, delete both incident JSON files, and verify `make release-check` succeeds with the proposed contract while an `rg` scan finds no live incident JSON reader or active replacement file.
- [x] 3.5 Remove exclusive npm scripts, Makefile targets and `.PHONY` entries, and `package-inputs.mjs` incident-file/script/test inputs; preserve the actual supported `release:check`, `release:candidate`, `release:transfer:create`, `release:transfer:validate`, fixture transfer, archive, consumer, and `registry:verify` commands and Makefile targets; verify `make help`, release contract tests, dependency-boundary tests, and input-selection tests pass.
- [x] 3.6 Delete incident-only `bootstrap-publish.test.mjs`, `bootstrap-recovery.test.mjs`, `bootstrap-recovery-cli.test.mjs` and their four `recovery-cli-*` fixtures only after confirming no surviving import; retain or replace generic metadata/verifier regressions and verify `npm run release:test` succeeds with no missing-loader fallback.

## 4. Documentation and specification alignment

- [x] 4.1 Rewrite the active operator path in `libs/frontend/RELEASE.md` for preparation-only interim operation and keep a compact historical section with original and partial release identities, exact package integrity/provenance, successful verification run, artifact expiry, and explicit remaining trust/revocation uncertainty; verify all repository links and historical values against inspected evidence.
- [x] 4.2 Update `libs/frontend/README.md` and only demonstrably stale RFC release-status text where affected by the cleanup; preserve broader RFC publication, FRED adoption, and RAGS work; verify no current documentation instructs dispatching retired options or assumes the three published coordinates are absent.
- [x] 4.3 Compare the cleanup's explicit `REMOVED` predecessor requirements and `ADDED` replacements against the predecessor-synchronized main spec; in a disposable OpenSpec root, semantically apply the retirement and verify old incident scenarios are gone while adoption, immutable evidence, generic verification, and every unaffected CI/token/UI/SDK guarantee remains. Keep task 12.7 and predecessor archival separate; external trust configuration is not a cleanup implementation prerequisite.

## 5. Integrated validation and review

- [x] 5.1 Under the pinned release Node/npm toolchain, run `make release-check`, `make release-test`, `make code-quality`, `make test`, and `make pack-check` in `libs/frontend`; verify all controlled release and existing token/UI/SDK archive positive/negative tests pass.
- [x] 5.2 Provision consumer caches and Chromium in separate network-capable steps; then run `make isolated-consumer`, `make browser-smoke`, and `make host-integration` with offline installation/no browser bootstrap during validation and the separate application toolchain where prescribed; verify all three package and host compatibility gates pass.
- [x] 5.3 Run focused workflow, fresh-process verifier, metadata retry, deletion-input selection, and retired-flag regressions; verify preparation can run with the two JSON files absent while malformed identities, unauthorized reads, signature/provenance mismatches, local fallback, and missing prerequisites still fail.
- [x] 5.4 Run `openspec validate retire-frontend-bootstrap-recovery --strict`, strict validation of the disposable post-retirement spec and the real predecessor-synchronized main/all specs, and `git diff --check`; verify no archival scenario-loss warning, dangling relative links, or active incident configuration references and a net reduction in incident-only production scripts/workflow jobs/commands. Report production/test/history removals separately.
- [x] 5.5 Obtain the repository-required independent implementation review, resolve in-scope findings, rerun affected gates, and leave the cleanup diff uncommitted/unpushed for review without registry or GitHub mutation.
