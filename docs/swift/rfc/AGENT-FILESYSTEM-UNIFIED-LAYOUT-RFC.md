# RFC Superseded: Unified Virtual Filesystem Layout

**Status:** superseded 2026-06-24
**Superseded by:** [`AGENT-FILESYSTEM-RFC.md`](AGENT-FILESYSTEM-RFC.md)
**Former tracked item:** `FILES-04`

This document is intentionally kept as a redirect for existing links.

The unified-layout decision is now folded into the single canonical filesystem RFC:

- user-facing four-root layout: `Resources`, `Mon espace`, `Espace d'equipe`, `Agents`;
- canonical team-rooted backend mapping;
- agent SDK path grammar;
- provenance;
- human-only share-by-copy;
- compatibility with the existing Knowledge Flow `/fs` product URL surface.

Do not implement from older copies of this RFC. Use
[`AGENT-FILESYSTEM-RFC.md`](AGENT-FILESYSTEM-RFC.md) instead.
