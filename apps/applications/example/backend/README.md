# Optional application backend

An independently built application service belongs in this directory when
`service_required` is `true`. This placeholder application is service-free, so
the directory contains no runtime code or build configuration.
