# Ignore artifacts:
build
dist
coverage
dockerfiles
.vite

# Ignore RTK Query slices generated from backend OpenAPI specs.
src/slices/**/**OpenApi.ts

# Ignore per-capability RTK Query slices and dumped schemas generated from
# capability OpenAPI specs (see `make update-<cap>-capability-api`).
src/rework/features/capabilities/**/api/**OpenApi.ts
src/rework/features/capabilities/**/api/openapi.json

# Ignore the vendored fred-agent-evaluator OpenAPI snapshot (cross-repo source
# of truth for evaluationOpenApi.ts — see src/slices/evaluation/README.md).
# Every other backend's raw openapi.json lives outside apps/frontend, so this
# is the only one prettier's `.` scan would otherwise reach.
src/slices/evaluation/openapi.json

# Same reason as evaluation's above: rags-services (#2307) lives in the
# sibling fred-rags repo, so its OpenAPI spec is copied into a local snapshot
# here for codegen — see src/slices/rags/README.md.
src/slices/rags/openapi.json
