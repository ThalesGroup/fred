# Copy to .env and adapt if needed

CONFIG_FILE=./config/configuration_prod.yaml

# Required only when security.m2m.enabled=true
KEYCLOAK_CONTROL_PLANE_CLIENT_SECRET=""
# Required only when security.rebac.enabled=true
OPENFGA_API_TOKEN=""
# Required when storage.content_storage.type=minio (for banner uploads)
MINIO_SECRET_KEY=""
# Required when storage.content_storage.url_strategy=proxy (signs team avatar URLs;
# `openssl rand -hex 32`, identical across replicas)
CONTROL_PLANE_CONTENT_URL_SECRET=""
# Postgres credentials
FRED_POSTGRES_PASSWORD=""
# Opensearch credentials
OPENSEARCH_PASSWORD=""