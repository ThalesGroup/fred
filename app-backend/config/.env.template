# app-backend startup contract
# - ENV_FILE points to this file (or another .env override)
# - CONFIG_FILE points to app-backend config yaml
# - This single env file is shared by embedded control-plane, agentic and
#   knowledge-flow backends in single-process mode.

CONFIG_FILE="./config/configuration.yaml"

# Example union entries for embedded mode:
# KEYCLOAK_CONTROL_PLANE_CLIENT_SECRET=
# KEYCLOAK_AGENTIC_CLIENT_SECRET=
# KEYCLOAK_KNOWLEDGE_FLOW_CLIENT_SECRET=
# OPENFGA_API_TOKEN=
