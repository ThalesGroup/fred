{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/ThalesGroup/fred/libs/frontend/release/fixture-transfer.schema.json",
  "title": "FRED frontend archive transfer metadata",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schemaVersion",
    "kind",
    "artifactName",
    "createdAt",
    "sourceCommit",
    "sourceTreeClean",
    "contract",
    "producerToolchain",
    "execution",
    "producerValidation",
    "packages"
  ],
  "properties": {
    "schemaVersion": { "const": 1 },
    "kind": {
      "enum": ["fixture-archive-transfer", "release-candidate-archive-transfer"]
    },
    "artifactName": { "type": "string", "minLength": 1 },
    "createdAt": { "type": "string", "format": "date-time" },
    "sourceCommit": { "type": "string", "pattern": "^[0-9a-f]{40}$" },
    "sourceTreeClean": { "type": "boolean" },
    "contract": {
      "type": "object",
      "additionalProperties": false,
      "required": ["state", "digest"],
      "properties": {
        "state": { "enum": ["fixture", "maintainer-confirmed"] },
        "digest": {
          "type": "string",
          "pattern": "^sha256-[A-Za-z0-9+/]{43}=$"
        }
      }
    },
    "producerToolchain": { "$ref": "#/$defs/toolchain" },
    "execution": {
      "type": "object",
      "additionalProperties": false,
      "required": ["provider", "repository", "workflow", "runId", "runAttempt"],
      "properties": {
        "provider": { "enum": ["github-actions", "local-rehearsal"] },
        "repository": { "type": "string", "minLength": 1 },
        "workflow": { "type": "string", "minLength": 1 },
        "runId": { "type": "string", "pattern": "^[A-Za-z0-9._-]+$" },
        "runAttempt": { "type": "string", "pattern": "^[1-9][0-9]*$" }
      }
    },
    "producerValidation": {
      "type": "object",
      "additionalProperties": false,
      "required": ["archives", "consumers", "browser", "host"],
      "properties": {
        "archives": { "const": true },
        "consumers": { "const": false },
        "browser": { "const": false },
        "host": { "const": false }
      }
    },
    "selectedIds": {
      "type": "array",
      "minItems": 1,
      "uniqueItems": true,
      "items": { "type": "string", "pattern": "^[a-z][A-Za-z0-9]*$" }
    },
    "packages": {
      "type": "object",
      "minProperties": 1,
      "propertyNames": { "pattern": "^[a-z][A-Za-z0-9]*$" },
      "additionalProperties": { "$ref": "#/$defs/package" }
    }
  },
  "$defs": {
    "toolchain": {
      "type": "object",
      "additionalProperties": false,
      "required": ["node", "npm"],
      "properties": {
        "node": { "type": "string", "pattern": "^[0-9]+\\.[0-9]+\\.[0-9]+$" },
        "npm": { "type": "string", "pattern": "^[0-9]+\\.[0-9]+\\.[0-9]+$" }
      }
    },
    "package": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "role",
        "name",
        "version",
        "coordinate",
        "filename",
        "bytes",
        "integrity"
      ],
      "properties": {
        "role": { "type": "string", "pattern": "^[a-z][A-Za-z0-9]*$" },
        "name": { "type": "string", "minLength": 1 },
        "version": { "type": "string", "minLength": 1 },
        "coordinate": { "type": "string", "minLength": 1 },
        "filename": { "type": "string", "pattern": "^[A-Za-z0-9._-]+\\.tgz$" },
        "bytes": { "type": "integer", "minimum": 1 },
        "integrity": {
          "type": "string",
          "pattern": "^sha512-[A-Za-z0-9+/]{86}==$"
        }
      }
    }
  }
}
